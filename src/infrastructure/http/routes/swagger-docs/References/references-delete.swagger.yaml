summary: Remove a work from the library
description: Also removes the tag from any notes that used it.
parameters:
  - name: id
    in: path
    required: true
    schema:
      type: integer
responses:
  '204':
    description: Reference deleted
  '404':
    description: Reference not found
  '500':
    description: Server error
